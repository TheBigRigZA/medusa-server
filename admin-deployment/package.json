{
  "name": "medusa-admin",
  "version": "1.0.0",
  "description": "Medusa Admin Dashboard",
  "scripts": {
    "build": "cd .. && yarn install && NODE_OPTIONS='--max-old-space-size=2048' npx medusa build && cp -r ../.medusa/admin ./dist",
    "start": "npx serve dist -l 8080"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}